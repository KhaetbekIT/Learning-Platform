import { Fragment } from "react";
import { Helmet } from "react-helmet";

const Home = () => {
  return (
    <Fragment>
      <Helmet>
        <title>Home</title>
      </Helmet>
      <section>home</section>
    </Fragment>
  );
};

export default Home;
